# チャット応答に「ミニダッシュボード応答」を追加

## 概要

チャット機能に、KPIカード列（`stat_cards`、前月比等の増減表示`delta`を含む）とドーナツチャート（`donut_chart`）という2つの新しいUIコンポーネントを追加し、「サマリー」または「ダッシュボード」キーワードを含むメッセージに対して、これら2コンポーネント＋既存の日別トレンドチャートから成る「ミニダッシュボード」応答を返します。

## 変更点

### バックエンド
- **新規ドメインモデル**: `StatCard`（`label`/`value`必須＋`delta`任意）、`StatCardsComponent`、`DonutChartComponent` を新規作成
- **UiComponent拡張**: `@JsonSubTypes` に新2type を追加し、計7種類の判別共用体へ
- **応答生成ロジック**: `KeywordMatchReplyGenerationAdapter` にダッシュボードシナリオ分岐を追加
  - トリガー: メッセージが「サマリー」または「ダッシュボード」を含む（新規問い合わせ/アラート不一致時）
  - 応答: 要約テキスト＋`[stat_cards, donut_chart, trend_chart]`の3コンポーネント
  - `trend_chart` は既存の日別シナリオから機能を抽出・共有
- **バックエンドテスト**: `ChatControllerTest` に新規テスト2件を追加（「今月のサマリーを見せて」「ダッシュボードを表示して」）

### フロントエンド
- **型定義追加**: `types/chat.ts` に `StatCard`、`StatCardsComponent`、`DonutChartComponent` を追加
- **検証ロジック追加**: `chatApi.ts` に新2type の必須フィールド検証関数を追加（`delta`は任意フィールドとして`undefined`/`null`/`string`を許容）
- **描画コンポーネント新規作成**:
  - `StatCardsView.vue`: KPIカードをグリッド表示。`delta`の符号により緑↑（増加）/赤↓（減少）/グレー通常（増減なし）に色分け
  - `DonutChartView.vue`: 自前SVG によるドーナツチャート（`stroke-dasharray` 方式。外部ライブラリ不使用）
- **MessageBubble.vue 統合**: 新2type の型ガード関数と描画分岐を追加

### ドキュメント
- `docs/api/chat-response-schema.md` に新2type のフィールド定義、サンプルJSON、トリガーキーワード、判定順序を追記
  - サンプルは実装に合わせて正確に記載し、既存シナリオ A～F との整合性を確認

## テスト結果

### 自動テスト
- バックエンド: `./mvnw test` **55/55 PASS**（既存テスト 12 + ダッシュボード新規 2 + 他の実装由来差分）
- フロントエンド: `npx vue-tsc -b` **PASS**（型エラーなし）
- フロントエンド: `npm run build` **PASS**（ビルド成功）
- API 動作確認: `curl` で ダッシュボード応答・既存 6 シナリオとも期待どおり返却を確認

### スキーマ文書との一致
- `docs/api/chat-response-schema.md` の新シナリオ G サンプルと、実際の API レスポンス（`stat_cards`/`donut_chart`/`trend_chart` の値、`delta` フィールド存在/省略パターン）が完全一致を確認

### 回帰確認
- 既存 9 テスト（担当者別・カテゴリ別・種別別・日別・新規問い合わせフロー・アラート・バリデーション）が全件パス。変更前後で応答内容に差なし

## 手動確認の残項目（ブラウザ必須）

以下 11 項目は backend/frontend の起動後、ブラウザで実際の UI 描画を確認する必要があります。コード上の根拠は確認済みで、実装上の懸念は見当たりません：

1. **AC-1（画面表示）**: 「今月のサマリーを見せて」送信 → KPIカード 4 枚 + ドーナツチャート + トレンドチャートが 1 吹き出し内に縦並びで表示されることを目視確認
2. **AC-2（画面表示）**: 「ダッシュボードを表示して」等の別言い回しでも同様に表示
3. **AC-3/AC-5（既存シナリオ非回帰）**: 「担当」「カテゴリ」「種別」「日別」キーワードの応答が本変更前と同内容であることを確認
4. **AC-6（未知type フォールバック）**: 一時的にコードを改変して未知type 受信を模擬 → エラー表示にフォールバック
5. **AC-7（必須フィールド不足フォールバック）**: `cards` 0件や `values` 件数不一致を模擬 → エラー表示へ
6. **AC-8（合計 0 時の安全性）**: `donut_chart` の合計値 0 を模擬 → 描画がクラッシュ・崩れしないことを確認
7. **AC-12（delta +で緑↑）**: KPI カード 1 枚目（`"+12%"`）が緑色・上矢印で表示
8. **AC-13（delta -で赤↓）**: KPI カード 2 枚目（`"-8%"`）が赤色・下矢印で表示
9. **AC-14（増減なし or 省略で通常表示）**: カード 3 枚目（`"±0pt"`）が矢印なしグレー表示、4 枚目（delta 省略）が矢印・文字列非表示
10. **AC-15（混在時の安全性）**: 上記 4 パターン混在でもクラッシュせず各々正しく描画
11. **AC-11（スキーマ文書一致）**: 実際のレスポンスと `chat-response-schema.md` 記載内容が一致することを確認

**手順**: `npm run dev`（port 5173）＋ `./mvnw spring-boot:run`（port 8080）を起動し、上記メッセージをチャット欄に送信して一覧の項目を目視確認してください。

---

Closes #6

🤖 Generated with [Claude Code](https://claude.com/claude-code)
